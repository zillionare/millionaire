# Millionaire UI — Story

本文件汇总所有面向用户、需要在 Web 界面中呈现或由用户操作触发的故事。**计算规则、调度规则、数据契约、撮合规则等业务规则不在本文件范围内**，仍由 [v0.2-001-strategy-framework](../v0.2-001-strategy-framework/story.md) 承载；本文件通过引用 001 的对应章节联动上游。

> **来源说明**：本文件的多数章节最初散落在 `v0.2-001-strategy-framework/story.md` 中，由 2026-06-17 的迁移整理集中到本文件。具体迁移记录见文件末尾"§19. 迁移记录"。

## 0. 系统壳层与认证

> **glm**：缺少用户进入系统之前/之后的全局壳层故事：未初始化 vs 已初始化、未登录 vs 已登录、登录页/退出登录/修改密码/个人设置、顶部导航/侧边栏、活动账户切换、告警中心。现有代码里这些能力已经是独立 UI 主线；如果 story 不先定义全局 shell，后续 spec 很难统一页面跳转、权限校验和布局约束。
>
>> **kilo**：本节集中定义全局 shell 的所有用户故事，包括系统初始化状态、登录/认证、顶部导航、侧边栏、活动账户切换、个人设置、告警中心。

### 0.1. 第一次打开系统

用户第一次在浏览器里打开 Millionaire 时，系统会判断自己是"刚装好"还是"已经配过了"。

- **刚装好**：用户被直接带入 init-wizard（§17），按引导设置管理员账号、运行环境、数据源等。Wizard 没走完之前，其它任何页面都打不开。
- **已经配过**：用户进入登录页，登录成功才进主界面。

### 0.2. 登录与个人设置

用户在登录页输入用户名和密码。如果连续输错次数太多，会被临时锁定并显示倒计时。登录成功后，会话保持若干小时；长时间不操作会自动登出。

> Aaron: 这一版不做临时锁定和超时自动登出。

用户可以：

- 在右上角用户菜单里点"退出登录"回到登录页
- 进"个人设置"改昵称、修改密码（要提供旧密码）、查看最近的登录记录

> Aaron: 不做查看最近的登录记录

任何需要登录才能看的页面被未登录用户访问时，会被自动重定向到登录页；登录成功后跳回用户原本想去的地方。

### 0.3. 主界面长什么样

登录后用户看到的主界面由三部分组成：

- **顶部**：左边是系统 Logo 和名称；中间是导航菜单；右边是告警中心入口（带未读数小红点）、用户菜单。
- **顶部导航菜单**有 Dashboard，策略，数据管理，系统管理，交易等入口。
- **左侧导航栏**：是顶部导航菜单的二级菜单。左侧导航栏也允许多级菜单。用户可以折叠或展开侧边栏，状态会被记住。
- **主内容区**：当前所选页面的内容。


### 0.5. 告警中心

系统运行过程中产生的所有需要用户关注的事情（数据同步错误、网关断连、风控事件等）都汇总到"告警中心"。用户从顶部导航中部进入，看到按时间排列的告警列表。

每条告警有来源、级别、摘要；用户可以标记已读、批量确认，或者点进去跳到告警来源的详情页。用户设置了"静默时段"的告警仍然会出现在列表里，只是被标记为"静默"。

> Aaron: 没有静默时段这个功能

## Dashboard

> Aaron: 暂定名，需要确定一个合适的中文名

提供关键信息的一站式访问。

用户在 dashboard 中，可以看到活跃账户的账户信息（实盘 + 仿真），计划任务，告警信息（分类显示，分策略和系统）。

## 1. 策略

用户通过该界面浏览当前的策略，运行回测，查看回测报告，将回测转仿真和实盘。

### 策略管理

- 用户可以列出系统中所有的用户策略和内建策略；
- 用户策略可能有更新。更新后能通过扫描（手动，界面触发）后，加载到策略显示列表（策略选择器？）
- 用户可以选择屏蔽某个策略（不删除，但不可见）。当他这样做时，该策略对应的所有回测结果也将不可见。
- 用户可以删除自定义的策略，当这样做时，所有关联的回测报告也将删除（不能删除正在进行实盘的策略，仿真可以）

### 回测管理

- 用户可以对策略进行回测。在回测前，他可以指定运行时参数和策略参数。
- 回测过程中，实时显示回测状态。在回测结束后，回测报告和日志自动保存
- 用户可以删除某次回测报告，或者全部回测报告。
- 通过选定某个策略，就可以看到该策略所有的回测报告（列表展示），并可以按年化收益、sharpe,sortino 和最大回撤进行排序
- 回测报告是一组报告，包括概览、每日成交、每日持仓、日志
- 概览要显示参数快照（运行时、策略参数，只读）、评估指标、净值曲线、交易信号、回撤图、月度热力图
- 每日成交要显示本次回测所有的成交记录，按时间排序，可导出 CSV
- 每日持仓要显示回测区间内每日的持仓快照，可按日期跳转
- 日志要显示回测运行的 stdout/stderr，可下载到本地
- 在回测报告中，日志可删除

### 仿真和实盘

- 用户可以在回测列表上操作，使它投入仿真或者实盘（遵循后台的调度约束），成为一个运行时实例
- 用户可以列出所有正在进行仿真的策略，并查看它们的运行详情（含账户资产、净值曲线、评估指标、订单事件、风控事件、心跳和错误）
- 在查看仿真策略的详情时，可以暂停或终止策略仿真
- 用户可以列出所有正在进行实盘的策略，并查看它们的运行详情（含账户资产、净值曲线、评估指标、订单事件、风控事件、心跳和错误）
- 在查看实盘策略的详情时，可以暂停或者终止策略实盘（这不等于停止实盘账户。一个实盘账户可能同时运行多个策略）
- 可以查看运行时实例的状态（运行中、暂停、终止）
- 暂停中的运行时实例可以再次启动
- 投入实盘的策略，在净值图上，要使用双 Y 轴显示净值曲线。主轴显示实盘 / dry-run 期间的真实净值，副轴显示并行仿真的模拟净值

> Aaron: 暂停（dry-run）是策略继续运行，但订单不会发出。终止是完全退出。之后策略必须以全新的实例再次运行。


## 7. 风控触发事件详情

风控策略在 [001 §1.9](../v0.2-001-strategy-framework/story.md) 触发卖出时，UI 除展示 §6 中的超额收益曲线外，还需展示每次触发的细节。

### 7.1. 触发事件列表

每条触发事件展示以下字段：

| 字段     | 来源                                                                                                          |
| -------- | ------------------------------------------------------------------------------------------------------------- |
| 时间戳   | 触发事件数据流                                                                                                |
| 标的     | 同上                                                                                                          |
| 触发价   | 同上                                                                                                          |
| 成本价   | 同上                                                                                                          |
| 原因     | `reason` 字段                                                                                                 |
| 超额收益 | 按 [001 §1.9 Triple Barrier](../v0.2-001-strategy-framework/story.md) 计算的 N 日窗口（默认 N=0，即当日收盘） |

### 7.2. 触发原因分布

- 饼图或柱状图：按 `reason` 字段分组
- 联动：点击某原因 → 过滤 §7.1 触发事件列表

## 8. 账户总览

- 汇总展示所有运行中策略的账户基本信息（总资产、可用现金、持仓市值、账户盈亏）
- 支持按策略筛选，查看单策略账户详情

## 9. 委托和成交记录

- 支持列出三态（回测、paper、live）下的委托记录，按策略、个股、时间段进行查询
- 支持列出三态下的成交记录，按策略、个股、时间段进行查询

## 10. paper / live 账户

- 每日收益率查询及净值曲线（可视化）
- 月度收益热力图
- 支持查询最大 N 亏损、最大 M 盈利个股
- paper / live 的账户基本信息（总资产、可用现金、持仓市值、账户盈亏）

> glm: §8-§10 更像“账户与结果展示”，但还缺“账户管理”故事：创建/删除/重置仿真账户、接入/检测实盘账户、选择活动账户、账户在线/离线状态、切换默认账户后的全站联动。当前代码里 `账号管理` 和 `活动账户选择` 已经是独立页面/流程，建议升格为单独章节。
>
>> **kilo**：新增 §10A 账户管理。

## 10A. 账户管理



### 10A.1. 三种账户

系统区分三种账户：仿真账户（用户自己管理的虚拟账户）、实盘账户（通过网关连到券商的真实账户）、风控账户（只能做风控，不能手动交易）。

### 10A.2. 仿真账户的创建、删除与重置

用户在将策略投入仿真时，将创建新的仿真账户。用户可以指定名字、确定本金。删除账户前要求账户处于空闲、且没有未结算的持仓；仿真账号没有重置账户。

### 10A.3. 实盘账户的接入

用户想接入一个实盘账户时，先要选一个已经配置好的网关，系统会立即检查这个网关是不是真的连得通、API Key 是不是有效、延迟在不在合理范围——只有通过了才能往下走。

实盘账户有三个状态：在线、离线、故障。离线时页面会显示告警横幅；网关恢复后账户自动回到在线状态，用户不需要做任何操作。



## 11. 实盘交易界面

界面顶部提供**策略选择器**，列出当前所有实盘运行的策略（日线 + 实时），用户可切换查看不同策略的信息。当用户没有选择策略时，显示总账户。但此时不可以进入买入操作。

1. 显示所选策略的账户基本信息
2. 显示所选策略的当日委托、成交并实时刷新
3. 提供基础交易界面，允许用户在紧急情况下手动交易
4. 手动交易必须归属一个目标策略（虚拟账户）；**风控策略不可作为手动交易目标**
5. 界面自动反映订单的委托状态变化。
6. 在当日委托中，可以进行撤单。

### 11.5.2. 下单时的辅助

用户在下单表单里下单时，系统会提供一些辅助信息帮助决策：

- 参考价：昨收、5 日均线、实时价可切换、以及在昨收基础上，提供多档涨跌速算价。
- 卖出时自动算"可卖数量"= 持仓 − 已挂单，并明确提示 T+1 约束
- 在持仓列表里双击即可卖出，并自动把标的、数量、价格带到下单表单
- 按金额反算股数（按 100 股取整）


### 11.5.4. 提交反馈

用户点"提交"后，会立刻看到反馈：

- 成功：toast 提示"已发送，订单号 xxx"，订单出现在当日委托列表顶部
- 失败：toast 红色提示 + 失败原因；表单保持原样不动，方便用户改完再提
- 网关离线：toast 提示"网关离线，订单已暂存"，订单先进入本地队列等网关恢复后自动发送

## 12. 仿真交易界面

界面顶部提供**策略选择器**，列出当前所有仿真运行的策略，用户可切换查看不同策略的信息。
同实盘交易界面，但不提供下单辅助。

## 系统管理
### 13. 任务调度与网关管理

- 用户可以看到所有数据同步任务（日线行情、证券列表、交易日历等）的列表
- 任务列表详情可以告诉用户任务多久跑一次，运行结果是什么，有没有出错。
- 用户可以：
  - 启用或禁用某个任务
  - 立即手动跑一次
  - 修改 cron 表达式
- 看每个任务最近 N 次执行的历史——成功还是失败、跑了多久、错误是什么。

### 13A.2. 网关管理

- 用户可以重新配置交易网关，包括服务器、端口、url_prefix 和 api KEY
- 用户可以看到网关的连接状态。
- 如果某个网关从健康变成异常，告警中心会立刻出现一条新告警。

### 14. 数据完整性校验报告展示

校验任务定义在 [001 §3.6](../v0.2-001-strategy-framework/story.md)。本节仅定义 UI 展示部分。

UI 展示校验报告：

- 缺日列表（交易日历缺失）
- 重复日列表
- 字段空值率（超过阈值时标红）
- 校验历史（最近 N 次）

### 15. 个股查询与 K 线图

查询能力在 [001 §3.7](../v0.2-001-strategy-framework/story.md)。本节仅定义 UI 表现：UI 提供按名字 / 数字 / 拼音模糊查询个股，选中后展示 K 线图（日线 + 关键字段 hover）。

## 事件通知

- 用户希望得到三种层面的事件通知：
  1. UI 界面及时展示并能自动消失的通知。
  2. 集中查看事件通知
  3. 通过 IM/邮件 收到事件通知。
- 可以定制哪些事件允许通知到 IM/邮件

微信通知通过**二维码扫描启用**：

1. 系统设置 → 通知 → 微信 → "启用"
2. UI 生成一次性二维码（含临时 token）
3. 用户扫码关注公众号并发送 token
4. 后台确认绑定后，UI 显示"已启用"

配置项：

- 启用 / 停用微信通知
- 事件订阅：可独立勾选"委托"、"成交"、"失败"、"网关断开"
- 静默时段：可选 22:00–08:00 不推送

## 17. 系统配置（init-wizard）

用户在安装后第一次打开系统时，会被强制带入 init-wizard。Wizard 一步一步引导用户完成系统启动所必需的配置：欢迎页 → 运行环境（监听地址、端口、URL 前缀、是否仅本机访问）→ 管理员账号（用户名 + 密码）→ 数据目录 → 数据源（一个或多个网关）→ 首次下载范围（交易日历起止、股票列表快照）→ 下载进度（实时显示每个任务的进度，可以选择跳过失败的）→ 通知渠道（可后置）→ 完成页。

Wizard 的入口对未初始化系统强制开放；对已初始化系统作为"重新配置"模式（§17.3），从系统设置进入。

### 17.2. 失败恢复

如果 wizard 任何一步失败（数据下载失败、网关连不上等），用户会停留在那一步，看到错误详情和"重试"按钮。用户可以重试，也可以选择"跳过该步骤"把它标记为待处理。已经成功的步骤不会被回退；用户重新进入 wizard 时会从失败的那步继续。

主界面顶部会用横幅提醒用户还有"待处理"的 wizard 步骤。

