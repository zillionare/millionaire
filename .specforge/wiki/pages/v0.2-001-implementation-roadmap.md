---
type: decision
title: v0.2-001 实施 roadmap (33 FR + 6 NFR)
date: 2026-06-23
agents: [Sisyphus (minimax m3)]
sources:
  - .specforge/project/v0.2-001-strategy-framework/{spec.md, spec-strategy.md, spec-trading.md, spec-foundation.md, acceptance.md, test-plan.md, interfaces.md}
  - quantide/ 当前实现 (134 Python 文件)
  - tests/e2e (24 test_*.py, P3 Step 2-A/B/C 基础设施)
related:
  - [[v0.2-001-sage-lex-cycle-1]]
  - [[.dev/memory/26-06-22]]
---

## 总结

v0.2-001 spec 锁定 + 39 issue (33 FR + 6 NFR) + verify-issue 全 pass 后, 进入实施阶段. 本文档定实施顺序 + 工作量.

## 现状评估 (2026-06-23)

| 维度 | 现状 | 来源 |
|------|------|------|
| quantide/ Python 文件 | 134 | `find quantide -name "*.py"` |
| 核心架构 (FR-010/013/020) | **已实现** | `quantide/core/strategy.py` (Strategy/BaseStrategy/RiskStrategy), `quantide/service/discovery.py` (StrategyLoader/enumerate_strategies) |
| PaperBroker (FR-010 simulator) | **已实现** | `quantide/service/sim_broker.py` (含 set_clock NFR-060) |
| NFR-060 RuntimeContext.clock | **已实施** | `quantide/core/runtime/modes.py` (RuntimeContext.clock + RuntimeBootstrap) |
| 24 e2e test_*.py | **已存在** | `tests/e2e/**/test_*.py` |
| e2e 基础设施 | **已建** | P3 Step 2-A/B/C: virtual_clock + runtime_factory + gateway_stub |
| FR-185 加权均价 | ❌ **未实施** | `grep "F-CB\|cost_basis"` 空 |

## 实际工作量评估

**真正需要新实施工作的 FR**:
- **FR-185 加权均价经典方案 A** (P1 已解, 算法定义清楚) — 唯一显著空白
- 部分 FR 边角 (e.g. FR-200 运行时参数滑点/手续费常量, FR-110 触发逻辑中 cost_basis 引用)

**已有代码 + 缺 issue 跟踪的 FR** (实施阶段是"验证 e2e 测试通过 + 关 issue"):
- FR-010/013/014/015/020 (核心架构)
- FR-115/125/130 (驱动契约, RuntimeContext 已有)
- FR-140~190 (交易规则, PaperBroker 已有大部分)
- FR-200~260 (运行时/调度, 已有)
- FR-360 (Triple Barrier, F-TB-1~5 公式 spec 锁定, 实现按公式)
- FR-440/450 (dry-run/通知, 框架已有)

**NFR**:
- NFR-010/020/030/040/050 — 由 CI 静态扫描 + ruff + mypy strict + coverage 保证
- NFR-060 — 已实施 (RuntimeContext.clock + VirtualClock + make_paper_runtime)

## 实施顺序 (Sisyphus 决定)

按依赖关系 + 风险 + spec 锁定度:

| PR | FR/NFR | 工作量 | 风险 | 验证 |
|----|--------|--------|------|------|
| **PR2** | **FR-185** 加权均价经典方案 A | 中 (1 文件 + 1 测试) | 低 (P1 已解, 算法清晰) | e2e L1 paper |
| PR3 | FR-140/150/160/170/180 交易规则 (在 PaperBroker + RiskStrategy 中实现/验证) | 大 | 中 | e2e paper/live |
| PR4 | FR-115/125/130 驱动契约验证 + FR-200/210/220/230/240/250 运行时 | 中 | 低 | e2e L1/L2 |
| PR5 | FR-360 Triple Barrier F-TB-1~5 公式 (spec F1 修了 /100) | 中 | 中 (Critical) | e2e paper |
| PR6 | FR-440 dry-run + FR-450 通知 | 中 | 低 | e2e live smoke |
| PR7 | FR-090/100/110 内置策略 (双均线/回落卖出/成本止损) | 大 | 中 | e2e paper |
| PR8 | FR-050/060/070/080 撮合规则 (test-plan §3.2 ground truth) | 中 | 中 | e2e paper/live |
| PR9 | NFR-010~050 (CI/code review) | 小 | 极低 | CI pipeline |
| PR10 | NFR-060 验收 (已有 AC-CLOCK-INJ-N 6 条, e2e L1 paper 用 VirtualClock 验证) | 小 | 低 | e2e L1 paper |
| PR11 | FR-360 (L1/L2 e2e 验证) + FR-440/450 (live smoke) | 中 | 中 | e2e L1/L2/L3 |

## PR2 试点 (FR-185 加权均价) — 当前

**实施步骤**:
1. PaperBroker 现有 buy/sell 处理 (L787) 加 cost_basis 加权均价逻辑
2. 4 条公式 (F-CB-1/2/3/4) 实施 + 边界 (同日反复买卖 / 部分卖出后买入 / 全部卖出清仓)
3. e2e 测试: `tests/e2e/paper/test_fr_185_cost_basis.py` — 4 公式 + 边界
4. 跑 e2e 测试, 通过
5. commit + push
6. 关 issue #77 (`gh issue close 77`)
7. Project status → pending verify (待 gh scope 修复后)

## 后续 PR 模式

每 PR 闭环:
1. 实施代码 (executor subagent, Sonnet)
2. code review (Sisyphus 自主, 跨 PR 一致性)
3. 跑 e2e 测试 (`pytest tests/e2e/...`)
4. 通过 → commit + push + 关 issue
5. 失败 → 修代码 + 重测
6. Project status → pending verify (gh scope 修复后批量关联)

## 关键约束

- 不破坏现有 24 e2e test_*.py (spec 锁定前已通过)
- 不 mock 框架内部 (test-plan §5.2.1 边界铁律)
- 用 P3 已建测试基础设施 (virtual_clock.py + runtime_factory.py)
- 每个 commit `Refs #N` / `Closes #N` 关联 issue
- 用 specforge 0.5.1 verify-issue 验证 (FR 已建 issue 格式合规)

## 风险

| 风险 | 缓解 |
|------|------|
| PaperBroker 现有 buy/sell 流程复杂 | FR-185 只在 L787 trade 处理时更新 pos.price, 不动其他逻辑 |
| 现有 24 e2e 测试被破坏 | PR2 commit 前跑全量 e2e, 失败先修 |
| 实施时间超出 | PR 拆细, 每个 PR 闭环 (实施 → review → 测试 → 关 issue), 不积压 |

## 下一步

- PR2: FR-185 加权均价 (当前, 用 executor subagent 实施)
- PR3: FR-140~180 交易规则
- ...
