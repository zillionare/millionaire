# Spec — 基础分册

- **Spec ID**: v0.2-001-strategy-framework
- **分册**: foundation (非功能需求 + 范围声明)
- **根 spec**: [spec.md](./spec.md)

## 非功能需求

---

### NFR-010 性能 — 1000 标的日线回测 < 60s

| 有效需求 | 可测性 | 是否已决定 |
| -------- | ------ | ---------- |
| ✅        | ✅      | ✅          |

1000 个 A 股日线、3 年区间、纯 Python 实现下，双均线回测端到端（含撮合）耗时 < 30s（CI 标准机）。

---

### NFR-020 类型完备

| 有效需求 | 可测性 | 是否已决定 |
| -------- | ------ | ---------- |
| ✅        | ✅      | ✅          |

所有公共 API 提供类型标注，mypy/pylance 严格模式 0 报错。

---

### NFR-030 单一职责原则

| 有效需求 | 可测性 | 是否已决定 |
| -------- | ------ | ---------- |
| ✅        | ✅      | ✅          |

每个方法 ≤ 50 行、最长 ≤ 120 行（不含 docstring 与注释）。

---

### NFR-040 不留冗余代码

| 有效需求 | 可测性 | 是否已决定 |
| -------- | ------ | ---------- |
| ✅        | ✅      | ✅          |

不保留"以备将来用"的代码；删除一次性 stub。

---

### NFR-050 可观测性契约（黑盒测试基础）

| 有效需求 | 可测性 | 是否已决定 |
| -------- | ------ | ---------- |
| ✅        | ✅      | ✅          |

凡 [acceptance.md](./acceptance.md) 验证需要的内部状态,实现层必须通过以下**至少一种**渠道将其**暴露为外部可观测点**,使黑盒测试可在不依赖内部实现的前提下完成验证:

| 渠道                         | 适用场景                                        |
| ---------------------------- | ----------------------------------------------- |
| 结构化日志条目               | 事件型状态(如枚举成功/跳过、回测进度、风控触发) |
| 数据存盘文件(JSON / Parquet) | 持久化状态(回测结果、账本快照、委托/成交明细)   |
| 数据库表                     | 关系型可查询状态                                |
| Web API endpoint             | 需要时序查询或远程访问的状态                    |

#### 不在本 NFR 范围

- 框架**内部实现细节**(类层次、调度状态机、中间数据结构)无需为可观测性而改造
- 本 NFR 只要求:验证 acceptance 所需的状态,**必须**有可观测出口

#### 联动

- [acceptance.md](./acceptance.md) — 每条 AC 应明确"通过哪个观测点验证"
- [test-plan.md](./test-plan.md) §3.1 — 维护一份**完整观测点清单**,作为实现层 PR 评审 checklist
- v0.2-002-ui — UI 渲染的可观测性另由 UI spec 覆盖

## 已知约束与排除项

### 约束

1. 项目使用 `uv` + `venv` 管理依赖；pyproject.toml 中已声明的库优先（不混用，如 `loguru` 与内置 logging 不混用）
2. 项目从未发布过，不考虑旧版本兼容
3. git remote owner (`zillionare`) 与 gh user (`quantclaws`) 不一致——v0.2 显式接受此状态

### 排除项（来自 story §12 限制声明）

1. **Millionaire 仅支持日线行情存储和回测**；实盘中配合 qmt-gateway 获得当日实时行情（1m/30m/1d），但不存储
2. **策略不能感知运行模式**（见 FR-040）
3. **Stop 单作为风控策略实现**，不作为单独订单类型
4. **Market-on-Close 暂不支持**

### 范围外产品

- **Billionaire / Zillionaire** 不在本仓库范围

### 推迟到 v0.3 / 后续 spec

- **AI Coding Agent skill**（基于 skill 封装数据/交易接口、生成策略模板）v0.3 实现（FR-010）。**注**：数据接口、交易接口本身属 v0.2 Python SDK（FR-010 已提供），仅 AI Agent 的 skill 封装层推迟
