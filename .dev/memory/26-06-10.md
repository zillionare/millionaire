# 26-06-10 会话日志

## 用户输入
- Aaron 报 #45 新评论 (https://github.com/zillionare/millionaire/issues/45#issuecomment-4659768402), 要求修复 Aaron 审查发现的 2 个 high-confidence 问题.

## Aaron 审查发现 (#45)
1. **wiring 缺失**: `GatewayBrokerWrapper.set_strategy_runtime_config()` 定义了但 `StrategyRuntime` 从未调用. `_strategy_cheat_on_close` 默认 True, 生产路径上 deferred_orders 分支实际是死代码. 测试只覆盖直接 wrapper 调用, 未覆盖 production 路径.
2. **post_auction 定价未实现**: spec 要求 `auction=昨收+滑点 / post_auction=次日开盘+滑点`, 但 `_estimate_limit_price()` 一律读昨收, 不分窗口. commit message 自己也写了 "留 TODO post_auction 用今开", 但 issue 已关闭.

## 修复
- commit `d1a96e8`: `fix(live-broker): #45 followup wiring + post_auction 定价 (Fixes #45)`
- 改动:
  - `quantide/service/strategy_runtime.py` 新增 `_apply_live_broker_config(runtime)` helper; `_strategy_loop` 在 live 模式启动前调一次, 把 strategy config 中的 `cheat_on_close`/`live_execution_window`/`live_execution_slippage` 注入到 broker wrapper
  - `quantide/core/runtime/gateway_broker.py` `_estimate_limit_price` 按 `execution_window` 分流: `auction` 读 `scheduled_at.date()-1` 的 close, `post_auction` 读 `scheduled_at.date()` 的 open
- 新增 4 个回归测试:
  - `test_apply_live_broker_config_wires_cheat_off_to_wrapper`: spy wrapper 验证调用
  - `test_apply_live_broker_config_defaults_when_strategy_omits_params`: 默认 cheat=False/auction/0.001
  - `test_live_estimate_limit_price_auction_uses_previous_close`: 10.0 × 1.001
  - `test_live_estimate_limit_price_post_auction_uses_next_day_open`: 11.5 × 1.002 + 验证 daily_bars.get_bars 的 end=scheduled_date

## 验证
- TDD: 4 RED → 4 GREEN
- related tests: 76/76 pass (`test_gateway_broker_adapter`, `test_strategy_runtime_manager`, `test_strategy_runner`, `test_gateway_accuracy`, `test_dual_ma_parity`, `test_forming_daily_bars`)
- 全测试: 621 passed, 5 failed (全部 pre-existing: clock_bridge/manifest/init_wizard_gateway), 3 errors (test_discovery test pollution). 与修复无关.

## 项目状态
- #45 重新开启 → 修复 → 关闭
- 项目 v0.1 status: #43-49 全部 Pending Verify
- 分支 releases/v0.1 push 到 origin, 从 `883eeab` → `d1a96e8`
---

# 26-06-10 会话日志 (续2)

## 用户输入
- Aaron 报 #46 / #47 / #49 三个新 review 评论. 注意 review 意见不能照单全收,要注意甄别.

## 评估结果
- **#46** 部分不同意: live/paper bar trigger = real time 是正确的 (不是 9:30/14:57), cheat_on_close 在 live 影响 broker 不是 trigger time. 但加 integration test 覆盖 production runtime path.
- **#47** 完全认同: paper modal 没渲染 config 表 + loader iteration 类型错误, 都修.
- **#49** 完全认同: cheat_on_close 只在内存, 重启后丢失, 修 (持久化到 _runtime_specs JSON).

## 修复 (commit 8054a94)
- #46: 不改 runtime loop, 加 test_strategy_loop_live_wires_broker_config_and_uses_real_time
- #47: _paper_deploy_modal 加 _render_config_table; _load_backtest_run_config 改 .get() + cls.default_config()
- #49: create_backtest_runtime 写 _runtime_specs 含 cheat 字段; _resolve_backtest_run 优先读持久化

## 测试 +5 RED → GREEN, 全测试 626 passed (5 pre-existing failure 与本次无关).

## 项目状态: #43-49 全部 Pending Verify, releases/v0.1 push 到 origin.
