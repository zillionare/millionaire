# 2026-05-13 对话记录

## 用户需求

1. 将“如何以尽量少的改动让系统更像 SPA”的分析整理成正式 spec 文档，供团队讨论。
2. 增加一条高优先级技术架构决定，避免后续新功能重新走回整页重刷路径。
3. 按现有 spec 风格，将文档实际写入仓库。

## 本轮处理

1. 新增 `.dev/specs/09-layout-fragment-navigation.md`，用于定义共享布局层的 fragment 导航方案、设计目标、实现边界和验收标准。
2. 在 `.dev/specs/00-architecture.md` 中追加“UI 导航与渲染架构约束”，将 fragment 导航提升为未来新功能的默认架构要求。
3. 文档内容明确区分 sidebar 的优先级与 header 的条件性接入，避免在讨论阶段把“小改动方案”误解为完整 SPA 改造。
4. 根据评审意见补充并收紧了 spec：默认采用 `hx-boost`、显式要求 URL/history 语义正确、定义 fragment 导航后的滚动规则，并明确禁止通过中间件解析 HTML 来实现导航分流。
5. 按 spec 做了第一轮代码试点：在 `MainLayout` 中加入 HTMX fragment 分流，为 `system` 侧边栏接入 `hx-boost` + `hx-push-url` + `outerHTML show:top`，并让 `system/*` 页面入口把 `req` 传入布局层，从而在共享壳内完成 `main content` 替换与 `sidebar` OOB 同步。
6. 新增了 `tests/web/pages/system/test_calendar.py` 与 `tests/web/pages/system/test_gateway.py` 的回归断言，并使用 `quantide` 环境执行 `pytest tests/web/pages/system` 以及定向的 `calendar/gateway` 测试，结果均通过。
7. 用户反馈 sidebar fragment 导航会把页面整体向上滚动，导致 sticky header 遮住部分 sidebar 与页面标题；随后将 `hx-swap` 从 `outerHTML show:top` 改为 `outerHTML show:none`，显式关闭 boosted navigation 的自动滚动，并重新通过 `calendar/gateway` 定向测试。
8. 用户随后要求直接提交代码；提交范围限定为本轮 fragment 导航 spec、共享布局改造、`system/*` 试点页面、对应回归测试以及对话 memory，不包含 `.kilo/` 下的临时规划文件。